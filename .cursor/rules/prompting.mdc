---
description: 
globs: 
alwaysApply: true
---
Service layer 에서 반환하는 객체는 response 로 네이밍합니다.
비즈니스 로직을 Service layer 에 구현해야 합니다.

ts interface (type) 정의는 ~.types.ts 로 네이밍 된 클래스에 구현합니다.

BigQuery 등 데이터베이스에 관련된 코드는 ~Repository.ts 로 네이밍 된 클래스에 구현합니다.

~_bigquery_table_schema_impl.md 로 네이밍 된 문서가 수정될 경우, devinfra 패키지의 bigquery 패키지 밑의 스키마를 수정하고, bigquery.tf 에 반영합니다.

불필요한 이모티컨은 포함하지 않습니다.
md 작성은 간결하게 합니다. 불필요한 내용을 포함하거나 장황하게 설명하지 않습니다.
md 작성시 강조 (**) 효과는 사용하지 않습니다.

대답이나 문서 작성은 반드시 한국말로 합니다.

코드의 주석, 또는 로그 작성은 한국말로 합니다.

Gemini 의 모델은 기본적으로 gemini-2.0-flash 를 사용합니다.